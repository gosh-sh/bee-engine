[package]
name = "bee-sdk"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
bee-miner = { path = "../bee_miner", default-features = false, features=["single-wasm"] }
